# AgentBridge

AgentBridge is a bridge service that connects Feishu to ACP-compatible agent clients, enabling non-technical users to ask questions directly in IM. It handles session lifecycle, context mounting, and routing automatically — so users never need to understand the underlying agent infrastructure.
